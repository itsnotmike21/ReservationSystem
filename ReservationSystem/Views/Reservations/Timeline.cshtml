@*@model IEnumerable<IGrouping<DateTime, ReservationSystem.Models.Reservation>>

@{
    ViewData["Title"] = "Kalendarz (Timeline)";
}

<h1 class="mb-4">Rezerwacje — Timeline</h1>

@foreach (var dayGroup in Model)
{
    <div class="card mb-4 shadow-sm">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <strong>@dayGroup.Key.ToString("dddd, dd MMMM yyyy")</strong>

            <a asp-controller="Reservations"
               asp-action="Create"
               asp-route-startDate="@dayGroup.Key.ToString("yyyy-MM-dd")"
               class="btn btn-light btn-sm">
                Zarezerwuj
            </a>
        </div>

        <ul class="list-group list-group-flush">

            @if (!dayGroup.Any())
            {
                <li class="list-group-item text-muted">Brak rezerwacji</li>
            }
            else
            {
                @foreach (var r in dayGroup)
                {
                    <li class="list-group-item">
                        <div class="d-flex justify-content-between">
                            <div>
                                <strong>@r.StartTime.ToString("HH:mm") – @r.EndTime.ToString("HH:mm")</strong><br />
                                <span>@r.Facility.Name</span>
                            </div>

                            <div>
                                @if (Context.Session.GetString("UserRole") == "Admin")
                                {
                                    <span class="text-muted">@r.User.Email</span>
                                }
                            </div>
                        </div>
                    </li>
                }
            }

        </ul>
    </div>
}*@